export default function SectionOutput({ handler }) {
    return (
        <>
            {handler.sectionData.map((formData) => {
                return (
                    <FormOutput
                        
                    />
                )
            })}
        </>
    )
}