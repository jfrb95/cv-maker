import Output from "./Output"

export default function FormOutput({ formData }) {
    return (
        <>
            {formData.map((outputData) => {
                return (
                    <Output
                        
                    />
                )
            })}
        </>
    )
}